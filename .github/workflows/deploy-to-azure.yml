# Azure App Service Deployment Workflow
# Deploys to app-accession-dev.azurewebsites.net on push to main
# Fast deploy (~1 min) - skips health checks by default
# Use workflow_dispatch with health_check=true for full verification

name: Deploy to Azure App Service

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      health_check:
        description: 'Run health check after deploy'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

env:
  AZURE_WEBAPP_NAME: app-accession-dev
  RESOURCE_GROUP: rg-accession-dev
  PYTHON_VERSION: '3.11'

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        cache: 'pip'

    - name: Create deployment package
      run: |
        # Fast zip without installing dependencies (Azure handles this)
        zip -r deploy.zip . \
          -x "*.git*" \
          -x "__pycache__/*" \
          -x "*.pyc" \
          -x "*.pyo" \
          -x ".env" \
          -x "venv/*" \
          -x ".venv/*" \
          -x "node_modules/*" \
          -x "*.log" \
          -x "webapp_logs*/*" \
          -x ".github/*" \
          -x "tests/*" \
          -x "deploy.zip"

        echo "📦 Package size: $(du -h deploy.zip | cut -f1)"

    - name: Login to Azure
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    - name: Deploy to Azure Web App
      run: |
        echo "🚀 Deploying to ${{ env.AZURE_WEBAPP_NAME }}..."

        az webapp deploy \
          --resource-group ${{ env.RESOURCE_GROUP }} \
          --name ${{ env.AZURE_WEBAPP_NAME }} \
          --src-path deploy.zip \
          --type zip \
          --async true

        echo ""
        echo "✅ Deployment package uploaded!"
        echo "📍 Site: https://${{ env.AZURE_WEBAPP_NAME }}.azurewebsites.net"
        echo ""
        echo "Note: Site will be available in ~1-2 minutes as container restarts"

    - name: Quick status check
      run: |
        echo "⏳ Waiting 20 seconds for deployment to register..."
        sleep 20

        # Single quick check - don't wait for full startup
        http_code=$(curl -s -o /dev/null -w "%{http_code}" \
          --connect-timeout 5 \
          --max-time 10 \
          https://${{ env.AZURE_WEBAPP_NAME }}.azurewebsites.net/health 2>/dev/null || echo "000")

        if [ "$http_code" = "200" ]; then
          echo "✅ Site already healthy!"
        elif [ "$http_code" = "503" ] || [ "$http_code" = "502" ]; then
          echo "🔄 Site restarting (expected) - will be ready in ~1 min"
        else
          echo "📡 Status: $http_code - deployment in progress"
        fi

    - name: Health check (optional)
      if: ${{ github.event.inputs.health_check == 'true' }}
      run: |
        echo "🏥 Running full health check..."
        echo "Waiting 60 seconds for container startup..."
        sleep 60

        max_attempts=10
        attempt=1

        while [ $attempt -le $max_attempts ]; do
          echo "Health check attempt $attempt/$max_attempts..."

          http_code=$(curl -s -o /dev/null -w "%{http_code}" \
            --connect-timeout 10 \
            --max-time 30 \
            https://${{ env.AZURE_WEBAPP_NAME }}.azurewebsites.net/health 2>/dev/null || echo "000")

          if [ "$http_code" = "200" ]; then
            echo "✅ Site is healthy!"
            exit 0
          fi

          echo "Status: $http_code - waiting 20s..."
          sleep 20
          attempt=$((attempt + 1))
        done

        echo "⚠️ Health check timed out - site may still be starting"
        exit 0

    - name: Azure logout
      if: always()
      run: az logout
